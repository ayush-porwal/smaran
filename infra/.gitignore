*.js
!jest.config.js
!eslint.config.js
*.d.ts
node_modules

# CDK asset staging directory
.cdk.staging
cdk.out

# LocalStack persisted state
.localstack

# Deploy workflow outputs (resource IDs, ARNs, etc.)
cdk-outputs.json
